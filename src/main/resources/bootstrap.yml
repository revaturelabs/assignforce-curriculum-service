spring:
  application:
    name: curriculum-service
  profiles:
    active: ${PROFILE:default}
  cloud:
    config:
      uri: ${CONFIG_URL:http://localhost:8888}
management:
  endpoints:
    web:
      exposure:
        include: health
