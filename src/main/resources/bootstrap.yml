spring:
  application:
    name: curriculum-service
  profiles:
    active: ${PROFILE:default}
  cloud:
    config:
      uri: ${CONFIG_URL:http://localhost:8888}
      name: curriculum-service
      profile: ${PROFILE}
management:
  endpoints:
    web:
      exposure:
        include: health
